server:
  port: 8080
  error:
    path: /error
  tomcat:
    uri-encoding: UTF-8
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test?characterEncoding=utf8&useSSL=false&useUnicode=true&serverTimezone=GMT%2B8
    username: root
    password: iamking888
    driver-class-name: com.mysql.cj.jdbc.Driver
    #schema-username: root
    #schema-password: iamking888
    #data-username: root
    #data-password: iamking888
    #initialization-mode: always
  #jpa:
    #database-platform: org.hibernate.dialect.MySQL5Dialect
    #hibernate:
    #  ddl-auto: validate
    #open-in-view: false
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
